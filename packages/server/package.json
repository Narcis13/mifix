{
  "name": "server",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "drizzle-orm": "^0.45.1",
    "hono": "^4.6.0",
    "mysql2": "^3.16.1",
    "shared": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "dotenv": "^17.2.3",
    "drizzle-kit": "^0.31.8",
    "typescript": "^5.7.0"
  }
}
